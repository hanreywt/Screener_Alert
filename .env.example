# Copy this file to .env and fill in your values.
#   cp .env.example .env
# The engine auto-loads .env on startup (see config.py).

# --- Discord (channel webhook) ---------------------------------------------
# Discord: Server Settings -> Integrations -> Webhooks -> New Webhook -> Copy URL
DISCORD_WEBHOOK_URL=

# --- Telegram (optional) ----------------------------------------------------
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
